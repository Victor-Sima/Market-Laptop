// src/products/products.admin.controller.ts
import { Controller } from '@nestjs/common';

@Controller('admin/products')
export class ProductsAdminController {
  // Vom adăuga endpoint-uri admin (edit, delete) în etapele următoare
}
